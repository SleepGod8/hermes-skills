# <域> · Agent 任务书（<DRI：姓名/角色>）

> **用途**：给 AI Agent（OpenCode/Claude Code/Hermes 等）实现 <域> 的完整任务指令。
> **执行模式**：<单 DRI / 共同负责>（共同负责时注明：矩阵按文件拆分避免冲突，实际互相补位）
> **前置必读**：`AGENTS.md` → `docs/02-roles/00-职责总览.md` → 角色页 → 本文档 → 引用契约。
> **权威源优先级**：本文档与契约冲突时，以 `docs/01-architecture/*`（冻结契约）+ `docs/_ref/*` 为准。

---

## 1. 负责内容（DRI 边界）

| 项 | 内容 |
|---|---|
| 角色 | <Agent 名> — <一句话职责> |
| 写权限 | <目录/文件列表> |
| 只读 | <他人域列表 + 归属> |
| 协作方 | <依赖谁/谁依赖你，走什么接口> |

## 2. 任务需求（原子任务）

| # | 任务 | Done 条件 |
|---|---|---|
| T1 | <任务> | <可验证的完成标准> |

<流程/分级/意图/权限等核心资产表格>

### 需要实现的 API（接口契约 §冻结）
```
<API 清单>
```

## 3. 所需技术栈

| 项 | 要求 |
|---|---|
| 语言/版本 | <如 Python 3.11 锁死> |
| 框架/依赖 | <列表> |
| 存储 | <表/缓存> |
| 关键约束 | <确定性校验/超时/降级/熔断> |

## 4. 核心资产（<规则/意图/参数>完整表）

<完整参数表——agent 一次拿到全部，不用翻源文件>

## 5. 代码做法（实现指引）

### 5.1 文件清单与现状
| 文件 | 现状 | 你要做的 |
|---|---|---|
| <路径> | <已有占位/不存在> | <实现内容> |

### 5.2 实现要点
1. <要点编号>
2. <要点编号>

## 6. 接入骨架（AgentBase · 4 个抽象方法）

```python
# <agent 文件>（结构示意）
<代码示意>
```

## 7. 边界（不许越界）

1. 文件级 DRI：一个文件一个 DRI；改别人文件必须 issue + DRI review
2. 禁止裸写存储/第三方——一律走 tool 层
3. 事件：只调 bus.emit()，禁止手写 publish
4. HTTP：只实现冻结路径，不新增私有端点
5. import 方向：api→service→tool/model→config/utils，禁反向

## 8. 红线（违反 = 返工）

1. 冻结契约只准加不准改；改 = 架构组+组长双签
2. 成功码=200；错误码只用冻结表（<域>拦截用 <code>）
3. <域特有红线：确定性/二次确认/事务/幂等/降级/脱敏>
4. 禁止 TODO/假成功；每写一个文件必须可运行 + pytest 绿
5. 诚实汇报：交付附可验证证据

## 9. 验收标准（Done 条件）

- [ ] <勾选清单>

## 10. 参考文档索引

| 文档 | 路径 | 用途 |
|---|---|---|
| 总章程 | `AGENTS.md` | 全局铁律 |
| 职责矩阵 | `docs/02-roles/00-职责总览.md` | DRI/边界 |
| 接口契约 | `docs/01-architecture/04-接口契约.md` | 响应/错误码/API |
| <其余契约> | ... | ... |

### ⚠️ 待确认事项（冻结会提出）
1. <交叉验证发现的差异点，每条含两方说法 + 建议>
